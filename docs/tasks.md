# Tasks / todo

This project is intentionally small and focused. The main workstreams are simple:

## Current priorities

- keep the library easy to understand
- keep the docs aligned with the actual API
- keep the demo focused on the real use case
- avoid turning the repo into a full app framework

## Useful follow-ups

- clean up version confusion across `v1` to `v4`
- make the demo more explicit about the expected runtime contract
- improve examples for search/filter and data-entry use cases
- keep README entries short and link to deeper docs instead of duplicating content

## Scope guardrails

If a change would:

- add a full app architecture
- add heavy validation or business logic
- replace the repo purpose with a larger framework

then it likely falls outside the project scope.

## Project intent

The repo should remain a metadata-driven, lightweight form renderer, not an application platform.
